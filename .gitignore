node_modules/
cdk.out/
.holmes/
.DS_Store
__pycache__/

# 브라우저로 화면을 확인할 때 나오는 스크린샷·스냅샷. 남길 화면은 walkthrough/에 둔다.
.playwright-mcp/
# 확인하다 리포 뿌리에 떨어진 스크린샷. 뿌리에만 걸린다 — walkthrough/와
# docs/architecture/의 그림은 남는다.
/*.png

# CDK가 VPC 조회 결과를 캐시하는 파일. 계정 번호가 들어가므로 공개 리포지토리에는 올리지 않는다.
# 없으면 배포할 때 다시 조회한다.
cdk.context.json

# 로컬에서 key-visual 을 실제 AWS 값으로 띄울 때 쓰는 파일.
# 루트를 서빙하는 개발 서버가 /aws-config.js 를 절대 경로로 찾기 때문에 여기에 둔다.
# 배포본은 CDK 가 S3 에 따로 만든다(Source.data) — 이 파일은 커밋하지 않는다.
/aws-config.js
