# 스토리 디벨롭 & 대본화 — 구현 로직 핸드오프

---

## 전체 파이프라인

시놉시스를 입력하면 7단계를 거쳐 대본이 완성되고, 세계관이 회차를 거듭할수록 누적 성장합니다.

```
시놉시스 입력 → 관계 그래프 추출 → 스토리 소재 탐지 → 제목·랭킹 자동 생성
→ 분기 제안 → 대본 생성 → 세계관 역기입 → 다음 라운드 (새 갈등 자동 발견)

```

모든 Bedrock 호출은 **비동기 패턴**(AppSync Event 모드 → Lambda 120초 → DynamoDB 결과 저장 → 브라우저 1.5초 폴링)으로 AppSync 30초 하드리밋을 우회합니다. 사용자가 선택한 모델(Haiku 4.5 / Sonnet 5 / Opus 4.8)이 전체 파이프라인에 일관되게 적용됩니다.

---

## 1. 관계 그래프 추출

### 무엇을 하는가

시놉시스나 대본 텍스트를 Bedrock Claude에 보내서, 등장인물·사건·비밀·장소·조직을 노드로, 관계(애정, 적대, 친족, 은폐 등)를 엣지로 자동 추출하여 Neptune DB에 저장합니다.

### 구현 로직

- **길이별 분기 처리**: 3,000자 미만이면 바로 추출, 3,000자 이상이면 **요약 → 추출** 2단계로 처리합니다. 긴 대본은 6,000자 단위로 조각내서 병렬 요약 후 이어 붙이고, 전체 요약본을 2,000자 이내로 압축한 뒤 추출합니다.
- **프롬프트 설계**: Claude에게 JSON-only 출력을 강제합니다. 노드에는 id, name, kind(Character/Event/Secret/Location/Faction)를 필수로, 엣지에는 주어(s), 술어(p), 목적어(o)와 tension(0~1) 등의 프로퍼티를 포함하도록 지시합니다. 동일 인물의 별명/호칭은 하나의 노드로 통합하라고 명시합니다.
- **검증 파이프라인**: LLM 출력은 그대로 쓰지 않고 `normalizeGraph`로 반드시 검증합니다. 존재하지 않는 노드를 참조하는 엣지 제거, 허용된 kind/술어 값만 통과, 중복 노드 병합, name이 없는 노드 제거 등 9개 검증 규칙을 적용합니다. 검증 경고는 개발자 콘솔에만 표시하고 사용자 화면에는 노출하지 않습니다.
- **파생 엣지 자동 생성**: 명시적으로 추출된 엣지에서 7개 규칙으로 파생 엣지를 자동 추가합니다. 예를 들어 A→B 짝사랑이 있는데 B→A가 없으면 "짝사랑(unrequited)" 태깅, A가 B의 비밀을 알고 C는 모르면 "A가 C에 대해 우위" 파생. 모든 파생 엣지는 `derived_by` 필드로 원본 규칙을 기록하여 명시/파생을 구분합니다.

### 아키텍처

- Bedrock Claude → 텍스트에서 그래프 JSON 추출 (ConverseCommand)
- Lambda (VPC 내) → 추출 오케스트레이션 + 검증
- Neptune (Gremlin :8182) → 그래프 저장
- Bedrock Interface Endpoint → VPC 내에서 NAT Gateway 없이 Bedrock 접근 

---

## 2. 스토리 소재 자동 탐지 (12개 프로브)

### 무엇을 하는가

추출된 관계 그래프에서 **드라마틱한 스토리 소재**를 자동으로 찾습니다. AI가 "관계의 빈틈"에서 다음 이야기를 제안하는 것이 핵심입니다.

### 구현 로직

- **12개 탐지 패턴(프로브)**: 각 프로브는 그래프의 노드/엣지 배열을 입력받아 특정 패턴을 찾는 순수 함수입니다. Bedrock을 호출하지 않고 코드 로직만으로 동작합니다.
- **감정 갈등**: 삼각관계(affection이 한 노드에 두 방향에서 수렴), 해소 안 된 긴장(tension이 높은데 함께 겪은 사건이 없음), 깨진 관계(화해도 결별도 기록 안 됨)
- **정보 비대칭**: 비밀의 칼자루(일부만 아는 비밀 → 권력 불균형), 드러날 위기의 비밀(비밀을 아는 사람이 점점 늘어남)
- **구조적 갈등**: 배신 가능 구조(조직 소속 + 비밀 보유), 권력 공백(리더가 위협받으면 후계 문제), 같은 것을 원하는 경쟁자
- **서사 부채**: 미회수 떡밥(결과 엣지가 없는 사건 = 대가를 치르지 않은 것), 숨겨진 연결(직접 관계 없지만 같은 사건/비밀로 이미 연결된 인물 쌍)
- **캐릭터 아크**: 폭발 임계점(한 인물에 tension > 0.7인 관계가 3개 이상 집중), 속죄 서사(과거 부정적 사건 + 현재 긍정적 관계)
- **스코어링**: 하드코딩이 아닌 각 탐지 결과에 base_weight × tension_factor × centrality_factor로 1차 점수를 매깁니다. 이 수학적 점수는 이후 LLM 리랭킹의 입력으로 사용됩니다.

### 아키텍처

- Neptune → 그래프 조회 (Gremlin 쿼리)
- Lambda → 프로브 실행 + 스코어링 (코드 로직, Bedrock 호출 없음)

---

## 3. 씨앗 제목·드라마 랭킹 (LLM 자동 생성)

### 무엇을 하는가

탐지된 소재들에 시놉시스 맥락에 맞는 **매력적인 제목**을 자동으로 짓고, **드라마적 매력도 순으로 재정렬**합니다.

### 구현 로직

- **제목 생성**: 기존에는 프로브별로 고정 타이틀 12개("치르지 않은 대가", "풀리지 않은 갈등" 등)였는데, 이제 LLM이 시놉시스 내용을 보고 각 소재에 맞는 고유한 제목을 짓습니다. 한 번의 Bedrock 호출로 전체 씨앗 제목을 일괄 생성합니다. 규칙: 10자 이내, 인물/사건명 포함, 작가가 클릭하고 싶어지는 톤, 같은 시놉시스 내 중복 금지.- 예: "치르지 않은 대가" (고정) → "학부모 모임의 후폭풍" (자동 생성)
- **드라마 랭킹**: 하드코딩된 가중치(escalation: 1.5, love_triangle: 1.2 등) 대신 LLM이 직접 순위를 판단합니다. 기존 수학적 스코어로 상위 10개를 추출한 뒤, LLM에게 "관객이 다음 회가 궁금해지는 정도, 캐릭터 갈등의 깊이, 반전 가능성" 기준으로 재정렬하도록 요청합니다. 각 소재에 랭킹 이유 한 줄이 함께 표시됩니다.
- **제목 생성과 랭킹은 병렬로 동시 호출**하여 대기 시간을 줄입니다.
- **기술 용어 필터**: LLM이 생성한 텍스트에 그래프 용어(노드, 엣지, 영문 술어 등)가 섞이면 자동으로 걸러냅니다. 프롬프트에도 "작가가 읽는 글에 프로그래밍/그래프 용어를 쓰지 말라" 규칙을 포함합니다.
- **폴백**: 제목 생성이나 랭킹이 실패하면 기존 고정 타이틀 + 수학적 스코어 순서로 자동 복구됩니다.
- **Race condition 방지**: LLM 리랭킹 응답이 도착하기 전에 사용자가 씨앗 카드를 클릭하면, 순서를 바꾸지 않고 제목만 업데이트합니다. 선택한 씨앗이 다른 위치로 점프하는 문제를 방지합니다.

### 아키텍처

- Bedrock Claude → 제목 생성 + 랭킹 (2개 호출 병렬)
- Lambda → 응답 파싱 + 필터링 + 폴백 처리

---

## 4. 분기 제안 (A/B/C 3가지 방향)

### 무엇을 하는가

사용자가 소재를 선택하면 AI가 **3가지 서로 다른 스토리 방향**을 제안합니다.

### 구현 로직

- 씨앗 정보(관련 인물, 사건, 갈등 구조) + 전체 세계관 그래프 컨텍스트를 Claude에 보내서 3개 분기를 생성합니다. 각 분기에는 제목, 톤, 전제(2~3문장), 비트(장면 구성 5개 내외), 인물별 상태 변화, 그리고 **writeback 데이터**(이 분기가 세계관에 어떤 변화를 일으키는지)가 포함됩니다.
- writeback 데이터가 핵심입니다 — 이 분기를 선택하면 어떤 인물/사건/관계가 추가/변경되는지가 여기에 명시되어 있고, 이것이 6단계에서 그래프에 반영됩니다.
- 사용자가 3개 방향 모두 마음에 들지 않으면, **자유 텍스트로 원하는 방향을 입력**해서 새로운 제안을 받을 수 있습니다.
- 프롬프트에 "작가가 읽는 글에 기술 용어·그래프 용어·영문 술어를 쓰지 말라" 규칙을 포함합니다.

### 아키텍처

- Bedrock Claude → 3개 분기 생성 (ConverseCommand)
- Lambda → 프롬프트 구성 + 응답 검증

---

## 5. 대본 생성

### 무엇을 하는가

선택한 분기를 실제 대본으로 변환합니다.

### 구현 로직

- 분기의 비트(beats)와 인물별 상태 변화(outcome)를 기존 대본 생성 파이프라인(개요 → 컷)에 연결합니다. 개요 프롬프트(outlinePrompt)로 에피소드 구조를 잡고, 컷 프롬프트(cutsPrompt)로 씬 단위 대본을 생성합니다.
- 출력 포맷: 씬번호(S#), 장소(INT/EXT), 시간대(낮/밤), 카메라 앵글, 등장인물, 지문, 대사
- 생성된 대본은 스토리보드에 컷 단위로 자동 배치됩니다.

### 아키텍처

- Bedrock Claude → 개요 생성 + 컷 생성 (2단계 체인)

---

## 6. 세계관 역기입 (Writeback) — 이 플랫폼의 핵심 차별점

### 무엇을 하는가

선택한 분기의 변경사항을 Neptune 관계 그래프에 적용하여, **회차를 거듭할수록 세계관이 자동으로 성장**합니다. 1화를 만들면 그 결과가 그래프에 반영되고, 2화를 만들 때는 1화에서 생긴 새로운 갈등과 관계가 자동으로 소재로 제안됩니다.

### 구현 로직

- **Writeback 데이터 구조**: 각 분기에 포함된 `{add_nodes, add_edges, update_props}` 데이터를 Neptune에 적용합니다. 새 인물·사건·관계가 그래프에 추가되고, 기존 관계가 변경됩니다.
- **검증**: 적용 전에 writeback 데이터를 검증합니다. 새 노드의 kind가 스키마에 있는지, 새 엣지의 from/to가 존재하는 노드인지, 기존 그래프와 모순되지 않는지 확인합니다.
- **고립 노드 방지 (3중 방어선)**: 역기입 시 기존 그래프와 연결되지 않는 "섬 노드"가 생기는 것을 방지합니다.1. 프롬프트 규칙: 분기 생성 시 "새 노드는 반드시 최소 1개 엣지로 기존 그래프에 연결" 지시

1. 자동 탐지: 역기입 데이터에서 어떤 엣지에도 닿지 않는 노드를 찾아냄
2. 자동 보정: kind별로 방향이 맞는 술어(Event→참여함, Secret→관련됨 등)로 가장 관련 있는 기존 인물과 자동 연결 + 경고 메시지 표시

- **누적 이력 저장**: 매 역기입마다 DynamoDB StoryHistory 테이블에 회차 번호, 분기 이름, 타임스탬프, 추가/변경/삭제 내역을 기록합니다. Sort key를 `<ISO 시각>#<분기 이름>`으로 구성하여 같은 시각의 다른 분기가 덮어쓰지 않도록 합니다.
- **Race condition 방지**: 첫 번째 역기입과 두 번째 역기입이 겹쳐 실행될 때 전역 변수를 다시 읽어 첫 번째가 유실되는 버그를 수정했습니다. 역기입 데이터를 지역 변수에 잡고, 브라우저 메모리(WB_ROUNDS)에도 회차를 폴백으로 유지합니다.

### 아키텍처

- Neptune → 그래프 업데이트 (Gremlin)
- DynamoDB → StoryHistory 테이블에 이력 저장
- Lambda → 검증 + 적용 + 고립 노드 보정

---

## 7. 세계관 챗봇 (World Navigator)

### 무엇을 하는가

사용자가 세계관에 대해 자유롭게 질문하면 현재 그래프 데이터를 기반으로 답변합니다.

### 구현 로직

- **프롬프트 구성**: Lambda가 Bedrock에 보내는 프롬프트에 현재 그래프 스냅샷 전체(노드 목록, 엣지 목록 — 한국어 변환 적용), 대화 히스토리, 역기입 누적 이력을 포함합니다.
- **한국어 자연어 답변**: 기존에는 그래프 데이터를 `재혁 --reveals--> 강회장 비리` 형태로 프롬프트에 넣어서 Claude가 그대로 베꼈습니다. 이제 엣지 데이터에 한국어 변환(`edgeKo`)을 적용하여 "재혁이 강회장의 비리를 폭로함" 형태로 넘기고, 시스템 프롬프트에 "그래프 용어를 쓰지 말고 한국어 자연어로 답하라" 규칙을 포함합니다.
- **누적 이력 답변**: StoryHistory 테이블에서 전체 역기입 이력을 조회하여 프롬프트에 포함합니다. "세계관이 몇 번 업데이트됐어?" 질문에 전체 회차 수와 각 회차 요약으로 정확히 답변합니다. 최근 10회차까지 포함하고 초과 시 "이전 N회차는 생략했다"고 표시합니다.
- **시스템 프롬프트 규칙 11개**: 그래프 용어 금지(#8), 누적 이력 질문 답변 방식(#9~11), 환각 방지(그래프에 있는 사실만 답할 것) 등

### 아키텍처

- Bedrock Claude → 답변 생성 (ConverseCommand)
- AppSync → 비동기 호출 (Event 모드, 30초 제한 우회)
- DynamoDB → 결과 저장(TTL 1시간) + StoryHistory 이력 조회
- Lambda → 그래프 컨텍스트 구성 + 이력 조회 + 프롬프트 조립

---

## 한국어 자연어화 레이어

### 무엇을 하는가

작가/PD가 보는 모든 화면에서 기술 용어를 제거하고 한국어 자연어로 표시합니다.

### 구현 로직

- **영어 술어 → 한국어 변환**: 37개 술어(loves, targets, conceals 등) 전체에 한국어 매핑 테이블을 구축하고, 목적어 받침을 보고 조사(을/를, 과/와, 이/가)를 자동 선택합니다.- 예: "재혁 → reveals → 강회장 비리" → "재혁이 강회장의 비리를 폭로함"
- **적용 범위**: 씨앗 카드, 분기 패널 역기입 목록, 노드 카드, 그래프 엣지 라벨, 역기입 미리보기/적용 기록 패널, 챗봇 응답 — 사용자에게 보이는 모든 곳에 일관 적용
- **차단 리스트**: "노드, 엣지, 역기입, 그래프, 술어, id, props, tension, kind" 및 영어 술어 원문이 사용자 화면에 나오면 안 됩니다. 개발자 경고는 console.warn으로만.
- **챗봇 프롬프트에도 적용**: Lambda가 Claude에게 보내는 그래프 데이터 자체를 한국어로 변환하여, Claude가 답변을 만들 때 영어 술어를 참조할 수 없게 합니다.

---

